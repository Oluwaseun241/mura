runtime: custom
env: flex

manual_scaling:
  instances: 1

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10

handlers:
  - url: /.*
    script: auto

health_check:
  enable_health_check: true
  check_interval_sec: 10
  timeout_sec: 50000
  unhealthy_threshold: 3
  healthy_threshold: 2
